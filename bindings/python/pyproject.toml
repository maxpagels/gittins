[build-system]
requires = ["maturin>=1.7,<2"]
build-backend = "maturin"

[project]
name = "gittins"
version = "0.1.0"
description = "gittins contextual bandit engine (Rust core, Python binding)"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
