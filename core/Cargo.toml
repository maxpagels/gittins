# The compiled core (Phase 1). Zero dependencies, like the reference: every
# byte of behavior is either std or written here, so the golden corpus is the
# only contract that matters.
[package]
name = "gittins-core"
version = "1.0.1"
edition = "2021"
description = "An opinionated, highly optimised contextual bandit engine."
license = "MIT"
repository = "https://github.com/maxpagels/gittins"
homepage = "https://docs.getgittins.dev"
documentation = "https://docs.getgittins.dev"
keywords = ["bandit", "contextual-bandit", "machine-learning", "online-learning", "exploration"]
categories = ["algorithms", "science"]

[dependencies]
